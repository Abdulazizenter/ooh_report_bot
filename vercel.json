{
  "version": 2,
  "buildCommand": "npm run build",
  "functions": {
    "server.js": {
      "runtime": "nodejs20.x",
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/server.js"
    },
    {
      "source": "/health",
      "destination": "/server.js"
    }
  ]
}
